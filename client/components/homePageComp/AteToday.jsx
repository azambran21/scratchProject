import React, { Component, useEffect, useState } from 'react';

const AteToday = () => {
  function fetchAte() {
    // promise chaining to grab the data from the database
    // method : 'POST'
    // parse the data
    // err handler
  }

  return (
    <div id="ateToday">
      <p> I ate this today </p>
    </div>
  );
};

export default AteToday;
